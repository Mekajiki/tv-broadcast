%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Status
      %th
  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.status
        %td= link_to 'Edit', edit_user_path(user)
